<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="notice_mapper">

	<!-- 글 작성 -->
	<insert id="insert_notice" parameterType="NoticeDto">
		INSERT INTO notice (notice_id, user_id, title, notice_content)
		VALUES ( notice_id_seq.NEXTVAL, #{userId}, #{title}, #{notice_content})
	</insert>

</mapper>